# Ionic-VueJS-for-GiveMeACar

**Pour initialiser le projet, pour le développement :**

 - se placer dans votre dossier  
 - `git clone ` 
 - `npm install`

**Pour tester le serveur Ionic :**

 - se placer dans votre dossier 
 -  `ionic serve`

*Packages requis :*
`
npm install -g @vue/cli `
```
npm install -g @ionic/cli
